# Disables Expo's standalone "React Native DevTools" Electron shell, which
# tries to download a large binary in the background on `expo start` and
# can crash the CLI in containerized environments (e.g. Codespaces) that
# can't launch a sandboxed Electron window. The debugger still works via
# the browser-based fallback; only the optional native shell is disabled.
# https://github.com/facebook/react-native/tree/main/packages/dev-middleware#unstable_experimentsenablestandalonefuseboxshell
EXPO_UNSTABLE_HEADLESS=1

# Supabase project for the backend migration (see supabase/migrations/).
# The EXPO_PUBLIC_ prefix is required for Expo to inline these into the
# client bundle. This is the publishable/anon key - safe to commit; it's
# rate-limited and gated by Row Level Security, not by secrecy. Never put
# the secret/service_role key here or anywhere client-side.
EXPO_PUBLIC_SUPABASE_URL=https://moevzrwbrlkzqwoianwh.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_rv6awH7H66Sr5weoMQxV1A_V3DJ9HcH
